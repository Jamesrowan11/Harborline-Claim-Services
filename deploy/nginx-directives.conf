# Optional additional nginx directives for Plesk Node.js hosting
# (Websites & Domains -> Apache & nginx Settings). Passenger already proxies
# the app; this only adds static-asset caching.
location /assets/ {
    expires 30d;
    add_header Cache-Control "public, immutable";
}
client_max_body_size 26m;
